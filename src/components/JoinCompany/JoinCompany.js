import React from 'react'
import {connect} from 'react-redux'

function JoinCompany (props) {
  
}

const mapStateToProps = reduxState => reduxState
export default connect (mapStateToProps)(JoinCompany)